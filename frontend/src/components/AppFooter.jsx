const AppFooter = ({ className = "" }) => (
  <footer className={`app-content text-center text-xs font-medium text-slate-500 ${className}`}>
    Made by Samiksha Shinde
  </footer>
);

export default AppFooter;
